# URL pública do backend (repositório separado clutchzone-backend), sem barra final.
# Passa a ser usada a partir da Fase 2 (Navbar + Auth), com fetch direto ao backend
# e cookies de sessão. Em dev, cai automaticamente em http://localhost:3001.
VITE_BACKEND_URL=http://localhost:3001
